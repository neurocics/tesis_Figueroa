%% cálculo de tiempo de evolución MS

% Datos de timeMS
timeMS = [14, 7, 4, 8, 7, 8, 12, 9, 20, 9, 11, 9, 6, 7, 4, 0.9, 7, 3];

% Cálculo del promedio timeMS
promedio = mean(timeMS);

% Cálculo de la desviación estándar timeMS
desviacion = std(timeMS);

% Mostrar los resultados
fprintf('Promedio: %.2f\n', promedio);
fprintf('Desviación estándar: %.2f\n', desviacion);

%% cálculo de edad MS

% Datos de edades MS
edadesMS = [53.9, 49.3, 32.6, 44.8, 31.3, 39.8, 46.3, 60.8, 43.9, 43.8, 43.0, 38.6, 45.2, 35.3, 27.9, 36.8, 32.7, 47.6, 32.3];

% Cálculo del promedio edades MS
promedio = mean(edadesMS);

% Cálculo de la desviación estándar timeMS
desviacion = std(edadesMS);

% Mostrar los resultados
fprintf('Promedio: %.2f\n', promedio);
fprintf('Desviación estándar: %.2f\n', desviacion);


%% %% cálculo de edad controles

% Datos de edades controles
edadesCON = [30.5, 34.6, 28.9, 34.0, 31.6, 34.8, 39.2, 51.9, 44.7, 43.5, 46.7, 36.8, 53.9, 38.7];

% Cálculo del promedio edades controles
promedio = mean(edadesCON);

% Cálculo de la desviación estándar timeMS
desviacion = std(edadesCON);

% Mostrar los resultados
fprintf('Promedio: %.2f\n', promedio);
fprintf('Desviación estándar: %.2f\n', desviacion);

%% %% cálculo años escolaridad MS

% Datos de escolaridad MS
    escolaMS = [18, 18, 14, 16, 16, 18, 18, 18, 18, 18, 18, 18, 18, 18, 18, 18, 18, 18, 18]
    
    % Cálculo del promedio escolaridad MS
promedio = mean(escolaMS);

% Cálculo de la desviación estándar timeMS
desviacion = std(escolaMS);

% Mostrar los resultados
fprintf('Promedio: %.2f\n', promedio);
fprintf('Desviación estándar: %.2f\n', desviacion);
    